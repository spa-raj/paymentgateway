apiVersion: v1
kind: Service
metadata:
  name: {{ include "paymentgateway.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "paymentgateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "paymentgateway.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
      protocol: TCP
